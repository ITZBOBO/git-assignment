# Git Assignment 2.0

**Student Name:** Enoch Agboola  
**Framework:** React Native (Expo Go)  

## Description
This repository contains my submission for the **Git Assignment 2.0**. It is a simple React Native application built with Expo Go. The default welcome screen has been modified as per the assignment instructions to display custom text, demonstrating basic knowledge of editing a React Native app and pushing it to a remote GitHub repository.

## Instructions Completed
- [x] Initialized a React Native Expo Go app.
- [x] Folder named correctly following the Last Name First Name format (Agboola Enoch).
- [x] Removed the default welcome text in App.js.
- [x] Added minor UI changes to display a custom welcome message and subtitle.
- [x] Pushed the code to a remote GitHub repository at least 1 day before submission.

## How to Run the App
1. Clone the repository:
   `ash
   git clone https://github.com/ITZBOBO/git-assignment.git
   `
2. Navigate to the project directory:
   `ash
   cd "Agboola Enoch"
   `
3. Install the dependencies:
   `ash
   npm install
   `
4. Start the Expo server:
   `ash
   npx expo start
   `
5. Open the Expo Go app on your phone and scan the QR code to view the project!
